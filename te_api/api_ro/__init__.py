# Auto-generated package. The contents (registry.py and per-tag modules)
# are produced by te_api.builder on first run. The __init__.py itself is
# intentionally empty so the package imports cleanly even before the
# build has happened.
